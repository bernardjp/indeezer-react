import { creaetStyles } from '@mantine/core';

type Props = {
  isHovered: boolean
}

const useStyles = createStyles(() => ({
  button: {

  },
  border: {

  }
}));

function PlaylistTrackControl(props): JSX.Element {
  return(

  )
}

export default PlaylistTrackControl;
