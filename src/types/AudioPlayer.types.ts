export type TrackType = {
  albumID: number,
  albumCover: string,
  albumThumbnail: string,
  albumTitle: string,
  artistID: number,
  artistName: string,
  explicitLyrics: boolean,
  source: string,
  trackID: number,
  trackTitle: string
}
